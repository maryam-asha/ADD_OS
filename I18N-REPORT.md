# I18N-REPORT.md — الثنائية اللغوية والمنسِّق الموحّد

> **المرحلة 5** · 2026-08-02 · قالب pinx-vue 1.23.0 · naive-ui 2.44.1
> **ما تحقّقت منه:** سلوك الوحدات في jsdom (46 اختباراً) ومخرجات البناء.
> **لم يُفتح متصفح.** كل ما يخصّ المظهر يبقى للفحص البشري — القسم 7.

---

## 1. آلية الربط لغة ↔ اتجاه

### المبدأ: الاتجاه دالّة في اللغة، لا مفتاح مستقلّ

```
   المدخل الوحيد القابل للكتابة
            │
      localesStore.locale ──────────────────────────────────┐
            │                                               │
            ▼                                               ▼
   bindDirectionToLocale (watch, immediate)          naiveuiLocale
            │                                        naiveuiDateLocale
   ┌────────┼──────────┬──────────────┐                     │
   ▼        ▼          ▼              ▼                     ▼
themeStore  <html      <html     currentLocale        n-config-provider
 .setRTL     dir>      lang>      (للمنسِّق)
   │
   ▼
body.direction-rtl  →  كتل .direction-rtl في SCSS
```

### الملفات

| الملف | الدور |
|---|---|
| [add-os/lang/locales.ts](src/add-os/lang/locales.ts) | `SUPPORTED_LOCALES` · `RTL_LOCALES` · `isRtlLocale` · `resolveLocale` — مصدر الحقيقة |
| [add-os/lang/bindDirectionToLocale.ts](src/add-os/lang/bindDirectionToLocale.ts) | `watch` بـ `immediate` يوزّع اللغة على أربع وجهات |
| [add-os/lang/currentLocale.ts](src/add-os/lang/currentLocale.ts) | `ref` يقرؤه المنسِّق خارج سياق المكوّنات |

يُركَّب مرة واحدة من `themeStore.startWatchers()`، التي يستدعيها `initTheme()` في
`Provider.vue` — وبحلولها يكون متجر اللغات حيّاً.

يستقبل الربط واجهة بنيوية `{ setRTL }` بدل استيراد متجر الثيم، لأن المتجر هو من يستدعيه.

### الحالة المتناقضة مستحيلة بالبناء

| المنفذ | كان | صار |
|---|---|---|
| مفتاح RTL في `LayoutSettings` | يكتب `setRTL` مباشرة ⟵ «عربي + LTR» بضغطة | يبدّل **اللغة** |
| `reset()` | `setRTL(false)` | `setLocale(DEFAULT_LOCALE)` |
| مبدّل اللغة | 7 لغات، منها ما لا اتجاه معرَّف له | `ar` / `en` فقط |

### الوميض عند إعادة التحميل

سكربت ما-قبل-الرسم في [index.html](index.html) يقرأ `__persisted__i18n` ويضبط
`lang`/`dir` قبل تحميل أي وحدة. بدونه، إعادة التحميل بالإنجليزية تعرض إطاراً RTL
قبل ترطيب Pinia.

قائمة لغات RTL مكرَّرة هناك **عمداً** — السكربت يسبق نظام الوحدات فلا يمكنه الاستيراد.
[اختبار](src/add-os/lang/__tests__/direction.spec.ts) يربط شكل الحفظ بما يقرأه السكربت،
فالانحراف يكسر الاختبار لا الواجهة.

### الحفظ — مقيس لا مفترض

`persist: { pick: ["locale"] }` كان موجوداً أصلاً في متجر i18n. تحقّقت منه باختبارَين:
الكتابة إلى `__persisted__i18n`، والاستعادة عبر نسخة Pinia جديدة تقرأ نفس التخزين.

---

## 2. حالة الترجمتين

### 2.1 اكتشاف غيّر فهم المهمة

**قالب Pinx لا يستخدم i18n لواجهته إطلاقاً.** المنظومة مركّبة بالكامل، لكن مفتاحين فقط
كانا موجودين فيها (`locales.*` و`sun_description`)، ولا يستهلكهما إلا مبدّل اللغة
وصفحة `MultiLanguage` التوضيحية. كل نصوص الواجهة مكتوبة إنجليزيةً داخل المكوّنات.

**الجواب على «هل كل مفتاح له قيمة إنجليزية؟» مزدوج:** تكافؤ المفاتيح الموجودة 100%،
وتغطية الواجهة صفر تقريباً.

### 2.2 جرد الفجوة — 39 نصّاً في 15 ملفاً

| الفئة | العدد | الحالة |
|---|---|---|
| 🔴 واجهة تشغيلية | 11 | ✅ **مترجَمة** (كان الجرد 10؛ ظهر نصّ حادي عشر أثناء التنفيذ) |
| 🟡 `LayoutSettings` | 5 | مؤجَّلة حتى يُحسم شحن أداة إعدادات القالب |
| ⚪ `auth/*` · `Navbar/*` · `Logo.vue` | 24 | تُترجَم مع إعادة كتابتها (المانيفست §2) |

النصّ الحادي عشر: `"Mark all as read"` في `Notifications/Toolbar.vue` — كان على سطر
مستقلّ فأفلت من نمط البحث `>...<`.

### 2.3 البنية

```
src/add-os/lang/
├── ar/index.ts + ar.json     الأساس ملكنا — لا عربية شحنها Pinx
├── en/index.ts + en.json     الأساس ملف القالب en.json + إضافاتنا
├── locales.ts · currentLocale.ts · bindDirectionToLocale.ts
└── __tests__/
```

[`src/lang/locales/index.ts`](src/lang/locales/index.ts) صار سطرين يشيران كلاهما إلى
`add-os/`. كل وحدة قادمة تضيف حزمتها إلى `ar/index.ts` و`en/index.ts` معاً.

### 2.4 المفاتيح الحالية

| المفتاح | ar | en |
|---|---|---|
| `common.search` · `close` · `save` · `delete` · `clear` · `viewAll` | بحث · إغلاق · حفظ · حذف · مسح · عرض الكل | Search · Close · Save · Delete · Clear · View all |
| `notifications.title` · `setAsRead` · `markAllAsRead` | الإشعارات · تعليم كمقروء · تعليم الكل كمقروء | Notifications · Set as read · Mark all as read |
| `shortcuts.title` | الاختصارات | Shortcuts |
| `locales.ar` · `locales.en` | العربية · الإنجليزية | Arabic · English |

### 2.5 الحارس الآلي

[`__tests__/messages.spec.ts`](src/add-os/lang/__tests__/messages.spec.ts) — 7 اختبارات:

| يمنع | كيف |
|---|---|
| مفتاح في لغة دون الأخرى | تطابق المفاتيح المسطّحة بأي عمق، + اختباران يسمّيان المفتاح الشاذ |
| فحصاً أجوف | يفشل إن كانت مجموعة المفاتيح فارغة |
| مفتاحاً بلا ترجمة | لا قيم فارغة |
| **نصّاً عربياً في حزمة `en`** | `\p{Script=Arabic}` — ترجمة حرفية لشرط «لا نصّ ينكشف» |
| **نسخ الإنجليزية دون ترجمة** | كل قيمة عربية يجب أن تحوي حرفاً عربياً |

`fallbackLocale: "en"` باقٍ **كشبكة أمان لا كاستراتيجية** — إن عمل، فالحارس فشل.

### 2.6 تقليص اللغات

حُذفت `de/es/fr/it/jp.json` (استثناء معتمد). السبب: `MessageSchema` مشتقّ من
`locales/index.ts`، فكل مفتاح وحدة كان سيتطلّب تكراره في ستة ملفات لإبقاء النوع سليماً.

نُظّفت البقايا: استيرادات naive-ui، `naiveuiLocales`، تحويل `jp → ja`، وlocales dayjs.
**نقاط التماس انخفضت من 21 إلى 18** — الحذف قلّص سطح التماس مع القالب.

---

## 3. المنسِّق الموحّد

### 3.1 القاعدة

> **كل رقم ومبلغ وتاريخ يُعرض للمستخدم يمرّ عبر [`@/add-os/utils/format`](src/add-os/utils/format/index.ts).**
> ممنوع خارجه: `toLocaleString` · `Intl.NumberFormat` · `Intl.DateTimeFormat` · `dayjs().format()` للعرض.

القرارات الثلاثة **تفشل كلها بصمت**، ولذلك القاعدة مطلقة:

| القرار | كيف ينكسر بصمت |
|---|---|
| أرقام لاتينية دائماً | `toLocaleString("ar-SY")` ⟵ `١٬٢٣٤` ولا أحد ينتبه |
| لا فقدان دقّة | `Intl` + `currency:"SYP"` ⟵ يحذف الكسور (CLDR: صفر منازل لليرة) |
| أشهر شامية | dayjs ⟵ «أغسطس» · date-fns `arDZ` ⟵ «أوت» |

### 3.2 الواجهة

```ts
import { formatCurrency, formatDate, formatNumber } from "@/add-os/utils/format"

formatNumber(1234567) // → "1,234,567"
formatCurrency(85000) // → "85,000 ل.س." (ar) · "85,000 SYP" (en)
formatCurrency("1234567.89") // → "1,234,567.89 ل.س." ← الكسر محفوظ
formatCurrency(1234.56, { fractionDigits: 0 }) // → "1,235 ل.س." ← تقريب مطلوب صراحةً
formatDate(d) // → "2 آب 2026" (ar) · "2 August 2026" (en)
formatDate(d, { style: "long" }) // → "الأحد، 2 آب 2026"
```

الأنماط: `iso` · `short` · `medium` · `long` · `monthYear` · `time` · `dateTime`.

اللغة تُشتقّ من `currentLocale` وهو `ref` — فاستدعاء `formatDate(x)` في قالب **يعيد
الرسم عند تبديل اللغة تلقائياً**، بلا composable ولا wiring.

### 3.3 الأرقام — لاتينية بحكم البناء

بُني على **معالجة نصية لا على `Intl`**. كل العمليات على محارف ASCII، فالأرقام اللاتينية
ليست إعداداً يمكن أن ينكسر بل **نتيجة بنيوية**.

يقبل `number | string | bigint`. **مرِّر قيم DECIMAL كنصوص** — النصّ يحفظ الدقّة مهما
طال؛ التحويل إلى `number` هو الخطوة المُفقِدة وتحدث قبل أن يصل المنسِّق.

يعالج: تدوين أُسّي (`1e21` ⟵ لا يُطبع كما هو) · `bigint` خارج المدى الآمن · `-0` ·
ويرفض `NaN`/`Infinity`/نصوص غير رقمية بدل تخمينها.

### 3.4 العملة — عدم فقدان الدقّة بالتصميم

```
Intl:      (1234567.89).toLocaleString("ar-SY", {style:"currency",currency:"SYP"})
              → "‏١٬٢٣٤٬٥٦٨ ل.س.‏"     أرقام خاطئة + .89 محذوفة بلا كلمة
ADD OS:    formatCurrency("1234567.89")
              → "1,234,567.89 ل.س."     الدقّة محفوظة
```

**قاعدة `fractionDigits`:** إن لم تُمرَّر —
- مبلغ صحيح ⟵ افتراضي العملة (صفر لليرة) — القرار المعتمد
- مبلغ بكسر ⟵ **الكسر يُعرض، لا يُقرَّب**

التقريب يحدث **فقط** حين يُطلب صراحةً. عدم التماثل مقصود: مع أعمدة DECIMAL، عرضٌ
يحذف أرقاماً بصمت أسوأ من عرض غير أنيق.

الرمز يتبع المبلغ في اللغتين. في فقرة RTL يضع خوارزم bidi المقطع اللاتيني أولاً
(يمين بصرياً)، فتُقرأ `1,234 ل.س.` صحيحة بلا علامات اتجاه مضمَّنة. **يحتاج تأكيداً بصرياً.**

### 3.5 التواريخ — جدول واحد

[`calendar.ts`](src/add-os/utils/format/calendar.ts) هو المصدر الوحيد:

**كانون الثاني · شباط · آذار · نيسان · أيّار · حزيران · تمّوز · آب · أيلول · تشرين الأول · تشرين الثاني · كانون الأول**

يُبنى التنسيق من دوال `Date` الرقمية + هذا الجدول — لا `Intl.DateTimeFormat`، ولا
locale من date-fns أو dayjs. أي أن الأرقام لاتينية والأشهر شامية **بحكم البناء**.

---

## 4. مدى الجدول — ثلاث وجهات

| الوجهة | الآلية | النتيجة |
|---|---|---|
| كل ما نعرضه | `formatDate` يقرأ الجدول | آب |
| أي كود يستخدم dayjs مباشرة | [`dayjsArabic.ts`](src/add-os/utils/format/dayjsArabic.ts) يحقن الجدول عبر `updateLocale`، مستدعىً من `utils/dayjs.ts` | آب (بدل أغسطس) |
| **لوحة `n-date-picker` الداخلية** | [`naiveDateLocale.ts`](src/add-os/utils/format/naiveDateLocale.ts) — انظر القسم 5 | آب (بدل أوت) |

---

## 5. منتقي التاريخ — القيد رُفع

**كان متوقَّعاً أن يبقى قيداً.** تبيّن أنه قابل للإصلاح.

`NDateLocale` هو `{ name, locale }` حيث `locale` كائن date-fns عادي، و`localize.month`
مجرّد دالة. فلُفَّ `dateArDZ` واستُبدلت تلك الدالة وحدها:

```ts
const localize = { ...base.localize, month: index => MONTHS.ar[index] }
export const dateArLevantine = { name: "ar", locale: { ...base, localize } }
```

كل ما عداه — أسماء الأيام، العصر، الترتيبيات، التحليل، `formatLong` — موروث من
`dateArDZ` بلا مساس؛ عربية فصحى مطابقة أصلاً. والأرقام تبقى لاتينية.

**بلا اعتماد جديد:** date-fns مقروء عبر تصدير naive-ui نفسه، لا مستورداً مباشرة
(المشروع لا يعلنه اعتماداً).

**التحقق** — [الاختبارات](src/add-os/utils/format/__tests__/format.spec.ts) تثبت الطرفين:

```
✅ baseline: dateArDZ.locale.localize.month(7, {width:"wide"}) === "أوت"
✅ dateArLevantine …………………………………………………………………… === "آب"
✅ الاثنا عشر شهراً تطابق formatDate حرفاً بحرف
✅ day / formatLong / match مراجع متطابقة مع arDZ (لم تُمسّ)
```

⚠️ يثبت هذا أن **الدالة التي يستدعيها المنتقي** تعيد الاسم الصحيح. **لم أرَ اللوحة
مرسومة.** يبقى للفحص البصري.

---

## 6. التحقق

| الفحص | النتيجة |
|---|---|
| `vue-tsc --build --force` | **18 خطأ** — `Kanban/ColumnEditor.vue` و`TaskEditor.vue`. الأساس دون تغيير عبر المرحلة كلها |
| `eslint .` | **خطآن** — `useNotifications.ts:129` و`utils/theme.ts:60`. ملفان لم يُمسّا |
| `vitest` | **46/46 ✅** — 9 اتجاه · 7 تكافؤ · 29 تنسيق · 1 قالب |
| `vite build` | ✅ |

**صفر أخطاء ناتجة عن المرحلة 5.**

---

## 7. يبقى للفحص البصري البشري

**لا شيء أدناه مؤكَّد بصرياً.**

1. **تبديل اللغة** — من لوحة الإعدادات: هل ينقلب الاتجاه حياً؟ ثم أعيدي التحميل:
   هل نجت اللغة؟ ثم «إعادة تعيين»: هل يعود إلى العربية لا الإنجليزية؟
2. **الوميض** — اضبطي الإنجليزية، أعيدي التحميل، وراقبي أول إطار: يجب ألا يظهر RTL لحظياً.
3. **النصوص المترجَمة** — الإشعارات · الاختصارات · البحث · قصّ الصور، في اللغتين.
4. **اتجاه المبالغ** — `formatCurrency` داخل فقرة عربية: هل تُقرأ `1,234 ل.س.` صحيحة،
   أم يحتاج الأمر علامة اتجاه مضمَّنة؟ (القسم 3.4)
5. **منتقي التاريخ** — افتحي `n-date-picker` بالعربية: هل يعرض «آب»؟ (القسم 5)
6. **التذييل** — `Aleppo Digital District © 2026`، بلا أثر لـ D*VERSE.
7. **الصور المحلية** — أفاتارات حوار البحث وغلاف `CardActions`: هل تظهر بلا طلب شبكة؟
   (تبويب Network يجب أن يخلو من `picsum` و`pravatar`.)

---

## 8. يحتاج قرار بشري

| # | البند | الوضع |
|---|---|---|
| 1 | فواصل الأرقام في العربية | استُخدم `,` و`.` اتساقاً مع «لاتينية دائماً». البديل `٬` و`٫` — ثابتان في [numbers.ts](src/add-os/utils/format/numbers.ts) |
| 2 | نمط الوقت | 24 ساعة. للتبديل إلى 12 ساعة: [dates.ts](src/add-os/utils/format/dates.ts) |
| 3 | موضع رمز العملة | بعد المبلغ في اللغتين. قابل للتغيير لكل عملة/لغة في [currency.ts](src/add-os/utils/format/currency.ts) |
| 4 | ترجمة `LayoutSettings` (5 نصوص) | مؤجَّلة حتى يُحسم شحن الأداة |
| 5 | إظهار لغات أخرى | `SUPPORTED_LOCALES` ثابت واحد — لكن يستلزم التزاماً بترجمة كل مفاتيح ADD OS إليها |
| 6 | نصّ قديم في صفحة توضيحية | [MultiLanguage.vue](src/views/MultiLanguage.vue) يوصي بـ `themeStore.setRTL(true)`، وهي نصيحة بطلت. صفحة فئة (ج) |
